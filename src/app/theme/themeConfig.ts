import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    // colorText: "#fff",
    colorPrimary: "#3498db",
    colorSuccess: "#2ecc71",
    colorWarning: "#f39c12",
    colorError: "#e74c3c",
  },
};

export default theme;
